

/updownso/Views/Home/Index.cshtml
- посмотри тут оборачивающий элемент <a> строка 49
на сайте он местами норм а местами клонируется внутрь <div class="product-description">
это видно если по-переворачиваешь карточки у продуктов. у некоторых будет голубым цветом описание...
и ещё это создает проблему нажатия на плей и карзину (у тех что yt голубые), он сразу переходит на страницу продукта, так как это линк.
цвет можно поменять окей.. а вот функциональность не почему глючит. может убрать пока открытие правой кнопкой.


--------------------------------------------------------------------------------------------------------------------


/updownso/Views/Home/Index.cshtml | m
Поменял местоположение линка + добавил линк на тайтле
/updownso/Content/css/default.css | m
/updownso/Content/Scripts/common.js | m  
Линк теперь только на описании продукта, которое на второй стороне карточки. И на нижней части, там где тайтл с ценой.
Так как это линк можно открывать правой кнопкой.


/updownso/images/basic/ | m 
обновил иконки, пока не подгонял.

----------------------------------------------------------------------------------------------------------
07.08.16
 UpDown/Content/css/default.css | m
Размер иконок сделал таким как и в файле 64х64, было 70х61 они сжимались и смотрелись размыто(сейчас немного четче)
В два раза уменьшил задержку на повороте карточки.
Вернул черный цвет линка на карточке.
Прижал меню в футере к верху, теперь везде одинаково стоит.

 UpDown/Content/css/media.css | m
Добавил отступы для кнопки закрытия меню в мобильной версии.

 UpDown/Scripts/common.js | m
В мобильной версии теперь при открытии меню скрывается кнопка меню. Тк. закрытие меню теперь внутри него.

 UpDown/Views/Products/Index.cshtml | m
 UpDown/Views/Products/RecommendedProducts.cshtml | m
Сделал карточки так же как и на главной.

 UpDown/Views/Shared/_Layout.cshtml | m
Добавил кнопку закрытия меню в мобильной версии.

-----------------------------------------------------------------------------------------------------------
09.08.16

 UpDown/Content/css/default.css | m
Исправил позиционирование всплывающего меню Search by и дропдаун валют

 UpDown/Views/Home/HeaderPartial.cshtml | m
Исправления для дропдаун валют
 

